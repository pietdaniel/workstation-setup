---
# Install neovim via Homebrew (macOS)

- name: Install neovim via Homebrew
  community.general.homebrew:
    name: nvim
    state: present

- name: Install yaml-language-server globally via npm
  community.general.npm:
    name: yaml-language-server
    global: true
    state: present
